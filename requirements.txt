# ── Web framework (let pip resolve compatible versions with gradio 6) ──────────
# cache-bust: 2026-03-06
fastapi>=0.115.2
uvicorn>=0.30.0
gradio==6.8.0
python-multipart>=0.0.18
pydantic>=2.0
httpx>=0.25.0
requests>=2.31.0
aiofiles>=22.0

# ── Vector store / LLM ────────────────────────────────────────────────────────
chromadb==1.5.2
ollama==0.6.1

# ── Document processing ───────────────────────────────────────────────────────
pypdf==4.3.1
pdf2image==1.17.0
pytesseract==0.3.13
Pillow>=10.0.0
openpyxl==3.1.5
python-docx==1.1.2
pandas==2.2.3

# ── Embeddings / ML ───────────────────────────────────────────────────────────
# torch is pre-installed CPU-only in the Dockerfile RUN step above
torch>=2.1.0
numpy>=1.24.0
transformers==4.49.0
sentence-transformers==5.2.3

# (no langchain packages needed — app uses chromadb and ollama directly)
